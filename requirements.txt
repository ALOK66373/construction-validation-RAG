# ============================================================
# requirements.txt — Python dependencies
# ============================================================
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Python 3.9+ required. Tested on 3.10 and 3.11.
# ============================================================

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0       # ASGI server to run FastAPI

# Data validation (used by FastAPI automatically)
pydantic>=2.0.0

# RAG components
sentence-transformers>=2.7.0    # Embedding model (all-MiniLM-L6-v2)
chromadb>=0.5.0                 # Vector database (local, persisted to disk)

# Groq SDK
groq>=0.11.0                    # Groq API client

# Utilities
python-dotenv>=1.0.0            # Load .env file for API keys
torch>=2.0.0                    # PyTorch backend for sentence-transformers

# Optional: speeds up sentence-transformers on CPU
# accelerate>=0.27.0
